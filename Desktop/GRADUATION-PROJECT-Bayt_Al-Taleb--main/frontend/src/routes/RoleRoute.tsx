/**
 * Role gate. Must be nested inside <ProtectedRoute>. Allows only the listed
 * roles; anyone else (authenticated but wrong role) is sent to /unauthorized.
 *
 *   <Route element={<RoleRoute allow={['ADMIN', 'SUPER_ADMIN']} />}> ... </Route>
 */

// ┌─────────────────────────────────────────────────────────────┐
// │ STUB — implement this file. (Reference impl in CodeDev.)     │
// └─────────────────────────────────────────────────────────────┘
// File: frontend/src/routes/RoleRoute.tsx
// Expected exports: RoleRoute
//
// TODO: implement.

export {};
